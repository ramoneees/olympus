# TEMPLATE — replace placeholder before applying
---
apiVersion: v1
kind: Secret
metadata:
  name: mariadb-secret
  namespace: databases
type: Opaque
stringData:
  mariadb-root-password: "<MARIADB_ROOT_PASSWORD>"
