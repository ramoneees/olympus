# TEMPLATE — replace placeholders before applying
---
apiVersion: v1
kind: Secret
metadata:
  name: postgresql-secret
  namespace: databases
type: Opaque
stringData:
  postgres-password: "<POSTGRES_ADMIN_PASSWORD>"
  replication-password: "<POSTGRES_REPLICATION_PASSWORD>"
