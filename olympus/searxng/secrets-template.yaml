# Template only — real values applied manually, never committed (see openwebui/secrets-template.yaml precedent)
apiVersion: v1
kind: Secret
metadata:
  name: searxng-secret
  namespace: olympus
type: Opaque
stringData:
  secret-key: REPLACE_ME_RANDOM # cookie-signing key, low sensitivity; any random string
