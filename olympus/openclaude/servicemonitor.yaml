---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: openclaude
  namespace: olympus
  labels:
    app: openclaude
spec:
  selector:
    matchLabels:
      app: openclaude
  namespaceSelector:
    matchNames:
      - olympus
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
