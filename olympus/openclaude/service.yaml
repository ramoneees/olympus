---
apiVersion: v1
kind: Service
metadata:
  name: openclaude
  namespace: olympus
  labels:
    app: openclaude
spec:
  type: ClusterIP
  selector:
    app: openclaude
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: grpc
      port: 50051
      targetPort: 50051
