{
  "name": "Daily Backup Health Digest",
  "nodes": [
    {
      "name": "Schedule Trigger",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [0, 0],
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "cronExpression",
              "expression": "0 5 * * *"
            }
          ]
        }
      }
    },
    {
      "name": "Fetch PG Backup Logs",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [224, 0],
      "parameters": {
        "language": "javaScript",
        "jsCode": "const fs = require('fs');\nlet token = '';\ntry {\n  token = fs.readFileSync('/var/run/secrets/kubernetes.io/serviceaccount/token', 'utf8').trim();\n} catch (e) {\n  return [{ json: { pg_logs: 'No K8s token: ' + e.message } }];\n}\n\nconst headers = { 'Authorization': 'Bearer ' + token };\nlet pgLogs = '';\n\ntry {\n  // Find most recent pg-backup job pods\n  const jobsResp = await fetch(\n    'https://kubernetes.default.svc/apis/batch/v1/namespaces/databases/jobs?labelSelector=app%3Dpg-backup',\n    { headers }\n  );\n  const jobsData = await jobsResp.json();\n  const jobs = (jobsData.items || [])\n    .sort((a, b) => new Date(b.metadata.creationTimestamp) - new Date(a.metadata.creationTimestamp))\n    .slice(0, 1);\n\n  for (const job of jobs) {\n    const podsResp = await fetch(\n      'https://kubernetes.default.svc/api/v1/namespaces/databases/pods?labelSelector=job-name%3D' + job.metadata.name,\n      { headers }\n    );\n    const podsData = await podsResp.json();\n    const pod = (podsData.items || [])[0];\n    if (pod) {\n      const logResp = await fetch(\n        'https://kubernetes.default.svc/api/v1/namespaces/databases/pods/' + pod.metadata.name + '/log',\n        { headers }\n      );\n      pgLogs = await logResp.text();\n    }\n  }\n} catch (e) {\n  pgLogs = 'Error fetching PG backup logs: ' + e.message;\n}\n\nreturn [{ json: { pg_logs: pgLogs || 'No pg-backup job found' } }];\n"
      }
    },
    {
      "name": "Fetch MariaDB Backup Logs",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [448, 0],
      "parameters": {
        "language": "javaScript",
        "jsCode": "const fs = require('fs');\nconst pgLogs = $input.first().json.pg_logs;\nlet token = '';\ntry {\n  token = fs.readFileSync('/var/run/secrets/kubernetes.io/serviceaccount/token', 'utf8').trim();\n} catch (e) {\n  return [{ json: { pg_logs: pgLogs, mariadb_logs: 'No K8s token: ' + e.message } }];\n}\n\nconst headers = { 'Authorization': 'Bearer ' + token };\nlet mariaLogs = '';\n\ntry {\n  const jobsResp = await fetch(\n    'https://kubernetes.default.svc/apis/batch/v1/namespaces/databases/jobs?labelSelector=app%3Dmariadb-backup',\n    { headers }\n  );\n  const jobsData = await jobsResp.json();\n  const jobs = (jobsData.items || [])\n    .sort((a, b) => new Date(b.metadata.creationTimestamp) - new Date(a.metadata.creationTimestamp))\n    .slice(0, 1);\n\n  for (const job of jobs) {\n    const podsResp = await fetch(\n      'https://kubernetes.default.svc/api/v1/namespaces/databases/pods?labelSelector=job-name%3D' + job.metadata.name,\n      { headers }\n    );\n    const podsData = await podsResp.json();\n    const pod = (podsData.items || [])[0];\n    if (pod) {\n      const logResp = await fetch(\n        'https://kubernetes.default.svc/api/v1/namespaces/databases/pods/' + pod.metadata.name + '/log',\n        { headers }\n      );\n      mariaLogs = await logResp.text();\n    }\n  }\n} catch (e) {\n  mariaLogs = 'Error fetching MariaDB backup logs: ' + e.message;\n}\n\nreturn [{ json: { pg_logs: pgLogs, mariadb_logs: mariaLogs || 'No mariadb-backup job found' } }];\n"
      }
    },
    {
      "name": "Fetch rclone Sync Logs",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [672, 0],
      "parameters": {
        "language": "javaScript",
        "jsCode": "const fs = require('fs');\nconst prev = $input.first().json;\nlet token = '';\ntry {\n  token = fs.readFileSync('/var/run/secrets/kubernetes.io/serviceaccount/token', 'utf8').trim();\n} catch (e) {\n  return [{ json: { ...prev, rclone_logs: 'No K8s token: ' + e.message } }];\n}\n\nconst headers = { 'Authorization': 'Bearer ' + token };\nlet rcloneLogs = '';\n\ntry {\n  const jobsResp = await fetch(\n    'https://kubernetes.default.svc/apis/batch/v1/namespaces/databases/jobs?labelSelector=app%3Drclone-sync',\n    { headers }\n  );\n  const jobsData = await jobsResp.json();\n  const jobs = (jobsData.items || [])\n    .sort((a, b) => new Date(b.metadata.creationTimestamp) - new Date(a.metadata.creationTimestamp))\n    .slice(0, 1);\n\n  for (const job of jobs) {\n    const podsResp = await fetch(\n      'https://kubernetes.default.svc/api/v1/namespaces/databases/pods?labelSelector=job-name%3D' + job.metadata.name,\n      { headers }\n    );\n    const podsData = await podsResp.json();\n    const pod = (podsData.items || [])[0];\n    if (pod) {\n      const logResp = await fetch(\n        'https://kubernetes.default.svc/api/v1/namespaces/databases/pods/' + pod.metadata.name + '/log',\n        { headers }\n      );\n      rcloneLogs = await logResp.text();\n    }\n  }\n} catch (e) {\n  rcloneLogs = 'Error fetching rclone logs: ' + e.message;\n}\n\nreturn [{ json: { ...prev, rclone_logs: rcloneLogs || 'No rclone-sync job found' } }];\n"
      }
    },
    {
      "name": "Call OpenClaude Digest",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [896, 0],
      "parameters": {
        "method": "POST",
        "url": "http://openclaude.olympus.svc.cluster.local:8080/review",
        "contentType": "json",
        "body": "={{ JSON.stringify({ diff: '=== PostgreSQL Backup Logs ===\\n' + $json.pg_logs + '\\n\\n=== MariaDB Backup Logs ===\\n' + $json.mariadb_logs + '\\n\\n=== rclone Sync Logs ===\\n' + $json.rclone_logs, depth: 'quick', pr_number: 0, commit_sha: '', repo_url: '', branch: '', prompt_type: 'backup-digest' }) }}",
        "options": {
          "timeout": 60000,
          "response": {
            "response": {
              "neverError": true
            }
          }
        }
      }
    },
    {
      "name": "Notify Hermes",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.2,
      "position": [1120, 0],
      "parameters": {
        "source": "database",
        "workflowId": "D1X5OyGlkuo7UwdR",
        "workflowInputs": "={{ JSON.stringify({ message: $json.review_text || $json.error || '⚠️ Backup digest generation failed — check manually', webhook_url: 'http://192.168.50.11:8644/webhooks/n8n-complete' }) }}",
        "options": { "waitForSubWorkflow": true }
      }
    }
  ],
  "connections": {
    "Schedule Trigger": { "main": [[{ "node": "Fetch PG Backup Logs" }]] },
    "Fetch PG Backup Logs": { "main": [[{ "node": "Fetch MariaDB Backup Logs" }]] },
    "Fetch MariaDB Backup Logs": { "main": [[{ "node": "Fetch rclone Sync Logs" }]] },
    "Fetch rclone Sync Logs": { "main": [[{ "node": "Call OpenClaude Digest" }]] },
    "Call OpenClaude Digest": { "main": [[{ "node": "Notify Hermes" }]] }
  },
  "active": false,
  "settings": {
    "saveManualExecutions": true,
    "callerPolicy": "any"
  }
}
