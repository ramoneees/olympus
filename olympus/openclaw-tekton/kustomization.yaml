apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - configmap.yaml
  - ingress.yaml
  - workspaces-configmap.yaml
  # secrets.yaml excluded — applied manually (not tracked in git)
