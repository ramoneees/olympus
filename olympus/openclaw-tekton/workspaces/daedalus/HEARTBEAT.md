# Heartbeat

On heartbeat, check for pending product tasks:

1. Review any new goals or projects that need specs
2. Check if delivered features need acceptance validation
3. Update backlog priorities if new information arrived

If nothing needs attention, reply HEARTBEAT_OK.
