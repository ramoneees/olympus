---
apiVersion: v1
kind: Secret
metadata:
  name: invoice-ninja-secret
  namespace: apps
type: Opaque
stringData:
  db-password: "inv_Cd9tY5mN7kF2"
  # Generate with: php artisan key:generate --show, or: echo "base64:$(head -c 32 /dev/urandom | base64)"
  app-key: "<INVOICE_NINJA_APP_KEY>"
