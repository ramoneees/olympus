# TEMPLATE — replace placeholder before applying
---
apiVersion: v1
kind: Secret
metadata:
  name: vaultwarden-secret
  namespace: apps
type: Opaque
stringData:
  admin-token: "<VAULTWARDEN_ADMIN_TOKEN>"
