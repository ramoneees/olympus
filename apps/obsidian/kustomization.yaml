apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - ingress.yaml
  # - secrets.yaml  # managed out-of-band — real values applied manually to cluster
  - configmap-sync.yaml
  - cronjob-sync.yaml
