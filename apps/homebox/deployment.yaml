---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: homebox
  namespace: apps
  labels:
    app: homebox
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: homebox
  template:
    metadata:
      labels:
        app: homebox
    spec:
      containers:
        - name: homebox
          # :latest is the only maintained tag (GHCR has no vX.Y.Z tags since 0.11.1);
          # pinned by digest = running v0.26.2. Bump digest on releases, verify via /api/v1/status.
          image: ghcr.io/sysadminsmedia/homebox:latest@sha256:b1ad7e3c63f732a5f6daa466e8116be4f545b3b120383a64dcb62beb00a660cc
          ports:
            - name: http
              containerPort: 7745
          env:
            - name: HBOX_WEB_MAX_UPLOAD_SIZE
              value: "50"
            - name: HBOX_OPTIONS_ALLOW_REGISTRATION
              value: "false"
            - name: HBOX_AUTH_API_KEY_PEPPER
              valueFrom:
                secretKeyRef:
                  name: homebox-secret
                  key: api-key-pepper
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              cpu: 25m
              memory: 128Mi
            limits:
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /api/v1/status
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /api/v1/status
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: homebox-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homebox-data
  namespace: apps
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
---
apiVersion: v1
kind: Service
metadata:
  name: homebox
  namespace: apps
spec:
  type: ClusterIP
  selector:
    app: homebox
  ports:
    - name: http
      port: 7745
      targetPort: 7745
