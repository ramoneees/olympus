---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: configarr-data
  namespace: apps
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
