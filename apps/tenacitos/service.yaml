apiVersion: v1
kind: Service
metadata:
  name: tenacitos-mission-control
  namespace: olympus
  labels:
    app: tenacitos-mission-control
spec:
  selector:
    app: tenacitos-mission-control
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP