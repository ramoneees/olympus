---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tend-website
  namespace: apps
  labels:
    app: tend-website
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: tend-website
  template:
    metadata:
      labels:
        app: tend-website
      annotations:
        # Reloader will roll the pod when the ConfigMap changes
        reloader.stakater.com/auto: "true"
    spec:
      containers:
        - name: nginx
          image: nginx:alpine
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              memory: 64Mi
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 30
            timeoutSeconds: 3
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 2
            periodSeconds: 10
            timeoutSeconds: 2
          volumeMounts:
            - name: content
              mountPath: /usr/share/nginx/html
              readOnly: true
            - name: content
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: nginx.conf
              readOnly: true
      volumes:
        - name: content
          configMap:
            name: tend-website-content
            items:
              - key: nginx.conf
                path: nginx.conf
              - key: index.html
                path: index.html
