apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - redis.yaml
  - ledger.yaml
  - backend.yaml
  - dashboard.yaml
  - ingress.yaml
# secrets.yaml excluded -- gitignored, applied manually (not tracked in git)
