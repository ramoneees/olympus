apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - elasticsearch.yaml
  - redis.yaml
  - deployment.yaml
  - ingress.yaml
# secrets.yaml is applied manually with `kubectl apply -f` — not tracked in
# git (see comment at the top of secrets.yaml).
