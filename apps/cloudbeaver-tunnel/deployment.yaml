---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudbeaver-tunnel
  namespace: apps
  labels:
    app: cloudbeaver-tunnel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloudbeaver-tunnel
  template:
    metadata:
      labels:
        app: cloudbeaver-tunnel
    spec:
      containers:
        - name: cloudflared
          image: cloudflare/cloudflared:latest
          args:
            - tunnel
            - --no-autoupdate
            - run
            - --token
            - $(TUNNEL_TOKEN)
          env:
            - name: TUNNEL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: cloudbeaver-tunnel-secret
                  key: tunnel-token
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              memory: 128Mi