# TEMPLATE — replace tunnel-token before applying
---
apiVersion: v1
kind: Secret
metadata:
  name: cloudbeaver-tunnel-secret
  namespace: apps
type: Opaque
stringData:
  tunnel-token: "REPLACE_WITH_CLOUDFLARE_TUNNEL_TOKEN"
