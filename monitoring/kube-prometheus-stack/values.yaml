# kube-prometheus-stack — Prometheus, Grafana, Alertmanager, node-exporter
# Tuned for 2-node homelab (NiPoGi N100 16GB + Ryzen 5600X 64GB)

grafana:
  enabled: true
  admin:
    existingSecret: grafana-admin
    userKey: admin-user
    passwordKey: admin-password
  replicas: 1  # single replica — homelab doesn't need HA Grafana
  persistence:
    enabled: true
    storageClassName: longhorn
    size: 5Gi
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      memory: 512Mi
  sidecar:
    datasources:
      enabled: true
      resources:
        requests:
          cpu: 10m
          memory: 32Mi
    dashboards:
      enabled: true
      resources:
        requests:
          cpu: 10m
          memory: 32Mi
  additionalDataSources:
    - name: Loki
      type: loki
      url: http://loki.monitoring.svc.cluster.local:3100
      access: proxy
      isDefault: false

prometheus:
  prometheusSpec:
    retention: 15d
    retentionSize: 12GB  # reduced — 40Gi PVC, 12GB cap leaves plenty of headroom
    resources:
      requests:
        cpu: 200m
        memory: 512Mi
      limits:
        memory: 2Gi
    storageSpec:
      volumeClaimTemplate:
        spec:
          storageClassName: longhorn
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 20Gi  # reduced from 40Gi — 15d retention + 12GB cap fits in 20Gi
    # Scrape all namespaces
    serviceMonitorSelectorNilUsesHelmValues: false
    podMonitorSelectorNilUsesHelmValues: false
    ruleSelectorNilUsesHelmValues: false

alertmanager:
  alertmanagerSpec:
    resources:
      requests:
        cpu: 50m
        memory: 64Mi
      limits:
        memory: 128Mi  # reduced — alertmanager uses <40Mi
    storage:
      volumeClaimTemplate:
        spec:
          storageClassName: longhorn
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 2Gi

  config:
    global:
      resolve_timeout: 5m
    route:
      group_by: ['alertname', 'namespace']
      group_wait: 10s
      group_interval: 10s
      receiver: 'n8n-webhook'
      routes:
        - match:
            severity: critical
          receiver: 'n8n-webhook'
          continue: false
        - match:
            severity: warning
          receiver: 'n8n-webhook'
          continue: false
    receivers:
      - name: 'null'
      - name: 'n8n-webhook'
        webhook_configs:
          - url: 'https://n8n.ramoneees.com/webhook/prometheus-alerts'
            send_resolved: true

nodeExporter:
  enabled: true

kubeStateMetrics:
  enabled: true

# Disable components that don't apply to k3s
kubeEtcd:
  enabled: false
kubeControllerManager:
  enabled: false
kubeScheduler:
  enabled: false
kubeProxy:
  enabled: false
