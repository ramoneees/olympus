---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana-tunnel
  namespace: monitoring
  labels:
    app: grafana-tunnel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grafana-tunnel
  template:
    metadata:
      labels:
        app: grafana-tunnel
    spec:
      containers:
        - name: cloudflared
          image: cloudflare/cloudflared:latest
          args:
            - tunnel
            - --no-autoupdate
            - run
            - --token
            - $(TUNNEL_TOKEN)
          env:
            - name: TUNNEL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: grafana-tunnel-secret
                  key: tunnel-token
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              memory: 128Mi