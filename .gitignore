passwords.txt
*.key
*.pem
.DS_Store
**/.DS_Store

# Secrets — never commit
.secrets/
*.env
*credentials*
*passwords*
*tokens*
**/*secret*.yaml
!infrastructure/secrets/
!infrastructure/secrets/agent-secrets-template/
!infrastructure/secrets/agent-secrets-template/*.yaml
!monitoring/**/*secrets*.yaml
openclaw-previous-config
.sisyphus

# olympus/openclaw/deployment.yaml — now tracked (moved from baremetal to K8s)
olympus/openclaw/configmap.yaml