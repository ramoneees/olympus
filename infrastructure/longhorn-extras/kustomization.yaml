apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ingress.yaml
  - mariadb-backup-recurring.yaml
  - pg-backup-recurring.yaml
  - redis-backup-recurring.yaml
# b2-credentials.yaml excluded — applied manually (contains secrets, not tracked in git)
